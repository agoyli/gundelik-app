# Gündelik Design System

Single source of truth: [`src/theme.ts`](src/theme.ts) (tokens) + [`src/components/Ui.tsx`](src/components/Ui.tsx) (primitives).
Extracted from the original Figma screens (Gündelik, Analitika, Gollanmalar, Testler, Algebra roadmap).

## Tokens

### Color
| Token | Value | Use |
|---|---|---|
| `blue` / `bluePress` | `#3570DF` / `#2758B8` | Primary fills, active states, hero stats |
| **`*Text` grade** — `blueText` `orangeText` `purpleText` `greenText` `tealText` `redText` | darker twins | **The same accent when it carries words.** ≥4.5:1 on white, on `surface`, and on its own tint |
| `blueSoft` / `blueTint` | `#DCE9FD` / `#EFF5FF` | Icon badges, tag pills, banners, grade bands |
| `surface` / `surfacePress` | `#F7F7F7` / `#EFEFEF` | Cards, rows, pill headers |
| `ink` `ink2` `ink3` `inkMuted` `inkDisabled` | grays | Text hierarchy (title → disabled) |
| `red` / `redDeep` / `redTint` | `#EA5455` / `#D22630` / `#FDE8E8` | Alerts, unread counters, "Ynsanperwer" |
| `green` / `greenDeep` / `greenTint` | `#2DD579` / `#22A06B` / `#E3F7ED` | Success grades, deltas, active plan tab, jump button |
| `orange` / `orangeTint` | `#F59B1B` / `#FDEFDC` | Points (bal) pill, coin |
| `purple` / `purpleTint` | `#A78BE0` / `#EDE6F9` | "Takyk" lesson kind |
| `teal` / `tealTint` | `#1FA58C` / `#DFF3EF` | Subject card (Jemgyýet) |
| `gold` `silver` `bronze` | — | Leaderboard medals, level progress fill |

**Two grades per accent.** `x` fills tiles, icons, borders and chart marks (WCAG needs only 3:1 there); `xText` is used the moment the accent becomes *text* at caption or body size. Never set a caption in the plain accent — that was the single most common contrast bug in the app. `KIND_META` carries both as `color` (fill) and `ink` (text).

The ink ramp is likewise all text-safe: `ink` → `ink2` (6.4:1) → `ink3` (5.5:1) → `inkMuted` (4.9:1). `inkDisabled` is for genuinely disabled controls only, which WCAG exempts.

### Radius
`rCard 24` (cards, tiles, pill header) · `rRow 16` (rows, buttons) · `rTile 12` (inputs, small tiles) · `rCell 8` (date cells) · `rPill 999` (tags, plan switch)

### Glass & elevation
- **Glass** (`blurBg` white / `blurBgSoft` lighter white / `blurTintBg` blue-tint + `blur`): every sticky or floating chrome surface is translucent with backdrop blur so content glimpses through — page headers, capsule headers, roadmap grade bands, the **tab bar** (floats over content; tab panels pad `104px + safe-area` to clear it, dropping to `16px` on inner pages where the bar is hidden), bottom **sheets** (`MuiDrawer` frosted paper), and the roadmap **jump button**. Full-screen overlays (lesson pages) stop above the tab bar so their footer CTAs stay reachable.
- **Shadows**: `shadowCtl` (raised header buttons) · `shadowFloat` (floating controls, jump button) · `shadowFab` (primary FAB, blue-tinted) · `shadowHeader` (soft downward fade under every sticky header — separates the glass from content scrolling beneath it). The continue card adds a a blue glow + gradient (`155deg, blue → bluePress`) as the one hero treatment.
- **Heatmap scale**: `heatBase → heat1 → heat2 → greenDeep` (Profil).

### Layout & type
Gutter `11px`, card padding `17px`. Type scale: h1 26/700, h2 20/700, h3 16/600, subtitle 17/600, body 15/14, caption 13. Numbers always `tabular-nums`.

### Accessibility floor
Every screen is audited against: **4.5:1** for text (3:1 for ≥24px, or ≥18.66px bold), **44×44** for icon-only controls, and `prefers-reduced-motion` (App-level GlobalStyles collapses every animation and transition). Text pills and chips stay at the 32px spec — wide enough to clear the WCAG AA 24×24 target minimum. An `ErrorBoundary` wraps each tab so a crash shows a Turkmen retry screen instead of a white void.

## Primitives (`src/components/Ui.tsx`)

| Component | Role | Used in |
|---|---|---|
| `SubPage` | **The sub-page frame**: `PillHeader` (which owns the swipe lock and the hidden nav) + gutter column. Every back-button page is built from it, so the back stack behaves identically on all of them | Gollanmalar sections, all detail pages, all Sazlamalar pages |
| `PillHeader` | Capsule page header, centred title, optional round back button. Calls `useSwipeLock()`, which is also what hides the tab bar — one signal for "this is an inner page" | every back-button page |
| `GridTile` | White card: icon badge + 17/700 label, 2-col grid | Gollanmalar |
| `SubjectTile` | `SubjectRow` folded into a 2-col grid: subject-tinted card, white badge in the subject's accent, 16/700 name, one value line. No progress bar — a half-width tile cannot read one | Öwrediji kartlar, Sapaklar |
| `ViewToggle` | The one list/grid control: a `HeaderIconButton` carrying the view you would *get*, never drawn pressed — it is an action, not a state | Sapaklar header |
| `Avatar` | The one student's face, at any size: initials on a tinted or brand-gradient disc, plus the **premium ring** (the subscription gradient with a gap of the page's own colour) when `usePrefs().premium`. Three screens used to draw their own circle and gave the same person three faces | Profil, Çagam, Testler |
| `IconBadge` | Squircle/round icon container (tint bg + accent icon) | tiles, subject cards |
| `TagPill` | Small tinted pill ("Ähli", "TOP-50") | section headings |
| `SectionHeading` | h2 + trailing pill row | Testler |
| `PointsPill` | Orange pill: coin + `N bal` | Testler profile, RankRow |
| `RankRow` | Medal + name/school + points | Reýting |
| `HeroStat` / `DeltaLine` | 34/700 blue figure + green ↑ caption | Analitika cards |
| `DoneBadge` | The one "completed" mark: `greenDeep` circle, white check, white ring | roadmap nodes & bands |
| `PeriodNav` | ‹ label › week/quarter switcher | Analitika |
| `SectionLabel` | Uppercase group label above a row list | Profil, Sazlamalar, sections |
| `SubPage`'s `help` prop | The replacement for `Lede`: a `?` button in the header corner opening a sheet. Explanation is available on demand instead of costing every visitor a paragraph they read once | Gollanmalar sections |
| `HelpButton` | **The one "?" in the app** — a `HeaderIconButton` with `QuestionOutlineIcon`. It was two: a 40px circle in `SubPage`'s header and a bare glyph on the diary's signature panel, the same affordance drawn two ways | `SubPage` header, Gündelik signature panel |
| `RowChevron` | The one "this row opens something" affordance | every navigating row |
| `ToggleSwitch` / `SwitchRow` | The one switch visual · the row that toggles it (row carries `role="switch"`, the knob is never a second tab stop) | Bildirişler, Howpsuzlyk |
| `Segmented` | 2–3 mutually exclusive options on one visible track. **Use instead of a cycling pill** whenever the alternatives must stay readable | Profil heatmap term switch |
| `HeatGrid` / `HeatLegend` | The one heat-map grid, driven by a `colorOf(r,c)` function. Grades use a sequential ramp, attendance three categorical states — the grid is shared so a cell size or a month header can only be changed in one place. **Both live in one card behind a switch**: two stacked cards drawing the same grid made the page look like it repeated itself, and flipping in place holds the cells still so the two maps stay comparable | Profil: Ýylyň kartasy |
| `StatTile` | Small figure + label tile, always in a 3-up grid | Çagam, Profil, every detail page |
| `Field` | Labelled input (`rTile` radius); read-only fields carry a `note` saying who owns the value | Profil maglumatlary, parol sheet |
| `StickyFooter` | Frosted footer holding a page's primary action above the tab bar | detail pages, edit form |
| `EmptyState` | The one "nothing here yet" shape: muted badge + title + note | Kartlarym, tests without attempts, Bellikledim |
| `BookmarkButton` | The one "save this" control, in a detail page's header. Filled = saved | every resource detail page |
| `MeterTile` | A `StatTile` for a figure with a known maximum — number, label, 4px track. Use whenever the reader would otherwise have to supply the scale | Profil indicators |
| `MonthCalendar` | The one date picker: Monday-first month grid, today ring, marked days, Sundays as days off | Gündelik date sheet |
| `GradeBadge` | **The only way a mark is ever drawn.** See below | lesson rows, lesson sheet |
| `SnapSlides` | **The one horizontally-snapping track**: full-width panels plus the dots, which are buttons. Three screens had grown their own copy and each scrolled differently; the track moves its own `scrollLeft` rather than calling `scrollIntoView`, which would drag the four-pane shell sideways | onboarding, premium promises, banner descriptions |
| `TodoRow` / `TodoList` | The one checkbox list: 22px box, task, what it belongs to, progress bar over the list. Read-only when no `onToggle` is passed, and `strike={false}` where the label is a *person* rather than a task | Öý işi sheet, lesson sheet, Synpdaşlar |
| `SurfaceRow`, `LessonCard`, `DateStrip`, `CountPill`, `SheetDrawer`, `SheetSection`, `TabBar` | Pre-existing core set | Gündelik & everywhere |

### Marks (`GradeBadge`)

A mark is the most-read number in the product, so it has exactly one shape and nothing else in the app may borrow it.

- **One mark, one cell.** `lesson.grade` is a `number | null`. It was a pair drawn as `5/4` behind a diagonal — two marks in one lesson is a rare case, and giving it the default shape made every ordinary lesson look like one.
- **The colour is derived from the digit**, never stored beside it: 5 green, 4 blue, 3 orange, 2 red (`GRADE_TONE`). A stored colour is a second source of truth that can disagree with the number it tints.
- **The mark's column is reserved on every row** (`GradeSlot`), whether or not there is a mark in it. Without it whatever else the row carries slides right on an ungraded lesson and lands exactly where a mark goes — a small circle with a number in it, in the mark's position, reads as a mark. (The unread disc that used to do this is gone; the reserved slot is what stops the next thing from doing it again.) Empty is drawn as a dash on a quiet fill, because "no mark yet" is what most of a school day looks like.
- **Solid fill, rounded square, 30×28.** The teacher's badge count (`BadgeScore`) sits next to it on the same row, so the two differ on *shape, weight and colour family*: the mark is a solid ink square, the badge a tinted pill. Two small tinted number chips side by side read as one control with two halves.
- Anything that is not a mark out of five — a subject average (`4.6`), a homework ratio (`3/5`), a points total — is **not** a `GradeBadge`. Averages are plain tabular figures; ratios stay `n/total` text.

### Paywall vocabulary (`src/components/Paywall.tsx`)

Five shapes cover every free-tier moment, so no screen writes its own `!premium &&` branch.

| Component | Role |
|---|---|
| `AdSlot` / `AdCard` | The **only** ad surface. It sells the subscription and nothing else, quotes `ENTRY` (the cheapest plan) rather than the dearest, and carries the `PROOF` numbers (10 000 mugallym her gün, 84 000 okuwçy, 312 mekdep). `AdSlot` renders nothing at all for premium users |
| `TeaserCard` | Locked-content block: a real `preview` behind `LockedPreview`'s blur, then title, one sentence, one CTA. Never a bare "Premium gerek" |
| `FreeLimitBar` | The weekly meter (tests, flash cards). Before use it states **what remains** ("Hepdede 1 mugt test"); after use, when it comes back ("Indiki duşenbe täzelenýär") |
| `PaidFeatureSheet` | For features with no partial state to preview — the AI helper. Sheet with three concrete bullets + the proof line |
| `PlanBadge` | Which plan a **child** is on and how long is left, as one pill: `show="plan"` names the tier for a list of children (`Zehinli · 28 gün`, `Adaty` with no countdown because the free plan does not end), `show="status"` says `Işjeň · 28 gün galdy` on a page already titled with the plan name. Green while there is time, orange under a fortnight, red once past. The days are counted from that child's `untilIso`; two screens used to print "28 gün galdy" as a literal beside a date they did not count from |
| `PremiumPill` / `BetaPill` | The two labels. Premium is blue-on-white on dark, Beta is purple |

## Patterns

- **Shell & navigation** ([`src/App.tsx`](src/App.tsx)): four tabs — Gündelik, Analitika, Gollanmalar, Profil — laid out on one horizontal **scroll-snap track**, so swiping left/right between them is driven by the OS (momentum, rubber-banding, mid-swipe cancel) rather than a JS animation. Scroll position is the source of truth; the tab bar mirrors it. Tapping the active tab scrolls that panel back to the top. Full-screen sub-pages call `useSwipeLock()` ([`SwipeLock.tsx`](src/components/SwipeLock.tsx)) so a sideways swipe inside them can never slide to another tab.
- **Everything that owns the screen mounts in [`PhoneFrame`](src/components/PhoneFrame.tsx)** — edge-to-edge on a handset, a 393×852 bezelled frame centred on anything wider. It is a component rather than a few lines inside the shell because the onboarding renders *before* the tabs exist: while the frame lived in `Shell`, the first screen a new family saw was the one screen that spread across a 1440px desktop as full-bleed white.
- **Header shape tells you where you are**: root tabs use `TopBar` (large left title + optional trailing action); anything reached by a back button uses the capsule `PillHeader`. Never mix them.
- **Gündelik** ([`GundelikScreen.tsx`](src/screens/GundelikScreen.tsx)) is a schedule, so the schedule gets the screen. Above it sit exactly two things: the date strip, and one **day-summary card** — three figures (bellik · nyşan · öý işi) split by hairlines, each cell a destination. It replaced three full-width rows carrying four coloured icon badges: ~190px of chrome for three numbers, which pushed the first lesson below the fold. Rules that came out of it:
  - **A count with a destination doesn't need a row — the count is the row.** If a row's only content is a label the icon already implies and a number, it is a cell.
  - **One action in the header, and it is the one about the whole screen.** Three icon buttons in a row read as a toolbar and invite a fourth. Only notifications qualify; the calendar moved to the end of the date strip — a control belongs beside the thing it changes — and sharing moved to the foot of the day, because you share a day after reading it.
  - **A scrollable strip has to say so.** The date strip is wider than the screen, so it carries a 26px mask fade at its right edge. A scrollbar it never shows and cells that happen to end at the edge are not an affordance.
  - **Notifications live in the header**, on the bell, as a `count` clipped to the button's corner — not as a full-width row competing with the day's actual content.
  - **The lesson row is a timetable row** ([`LessonCard`](src/components/Ui.tsx)): a left rail of aligned start/end times (what you actually scan a schedule by, and cheaper than a clock icon on every row), the subject, and the real `tema` as text. No "Tema" / "Öý işi" labels — a label identical on every card looks like data and carries none. Homework is one pill that appears only when there is homework, and **it is a control**: tapping it ticks the assignment off without opening anything. The pill's text is always the assignment — replacing it with "Öý işi taýýar" threw away the one thing the row is for and made the two states look like two different rows. State is carried by the tick, the colour and a strike-through. Because a button cannot nest inside a button, the card is a `Box` holding two `ButtonBase`es rather than one.
  - **A lesson row ends with what the teacher recorded** — the badge score, then the mark. It used to also carry a red disc counting unread notes: a red circle with a digit in it, one slot from the mark, which every reader tried to read as a mark. The count was never wrong, it was in the wrong place. It has a better home in the day-summary card's **Bellik** cell, where it is labelled and opens the notes themselves.
  - **Today keeps a ring in the date strip** when it is not the selected day, so a reader who has browsed backwards can find "now" without counting.
  - **A day cell carries one marker, and it means one thing.** There used to be three — a house, a pen and a dot — which readers took for "holiday" and "celebration", neither of which the app knows. The dot means the day has been signed. Şenbe is a school day and is never greyed out.
  - **Barla is a parent's signature, not a data refresh.** It applies to the selected day, the panel turns green and stops asking once signed, and the signature shows as that day's dot in both the strip and the calendar. A button that stays lit after being pressed teaches the reader that pressing it did nothing.
- **Sub-navigation inside a tab**: screen keeps a `view` state, each sub-view renders `PillHeader` with `onBack`.
- **Gollanmalar** is five sections: Temalar · Öwrediji kartlar · Testler · Bäsleşikler · Kitaphana. Each has its own page in [`SectionScreens.tsx`](src/screens/SectionScreens.tsx) built from the same shape — the list, with the page's explanation behind the header's `?` rather than in a lede above the content. Temalar is the entry to the lesson roadmap.
  - **No section leads with a featured banner.** Every page used to open with a tinted card repeating the first row of the list beneath it in a louder voice ("most played", "continue", "12 cards waiting"). That is a promotion, not navigation. **The list is the page.**
  - **Oýunlar is not a section.** A game practises a topic, so it hangs off Temalar under *Interaktiw*, beside the topics it drills — as its own tile it sat as far from that topic as the information architecture allows.
  - **Sapaklar is readable two ways, and the reader chooses** ([`ViewToggle`](src/components/Ui.tsx) in the header, remembered in `prefs.subjectView` because a way of looking is not a one-visit decision). The row is the considered view — it has the width for what the bank holds and for progress. The card is the scanning view — seventeen subjects in two screens instead of five, each in its own colour, for when you already know which subject you want and are looking for it by sight. Each carries the line it has room for rather than a squeezed copy of the other's. **The header holds one control beside the "?"**: at 375px the capsule fits a back button, a centred title and ~96px of controls, so the two-glyph segmented switch this started as clipped the word *Sapaklar*.
  - **Bäsleşikler holds two kinds, on one `Segmented`.** *Olimpiýadalar* is the school's own: sat on paper, in a hall, run by the school or the ministry — by the time it reaches a phone it is a **record**, not a thing to enter ([`Olympiad`](src/data/guides.tsx) → [`OlympiadDetailScreen`](src/screens/DetailScreens.tsx)). *Onlaýn bäsleşikler* is the season-long kind — it opens on a date, runs on the work you do across the app, and hands out objects that named sponsors put up ([`PrizeContest`](src/data/guides.tsx) → [`PrizeContestScreen`](src/screens/DetailScreens.tsx)). Both use **one card** with one footer strip and one slot swapped, so switching tabs does not look like changing app. The two tabs answer two questions — *how did we do* and *what can I enter*.
    - **An olympiad has no points, no tests and no CTA.** It carries a stage (mekdep · etrap · welaýat · döwlet), a date, and **percent results per pupil** — the one figure that means the same thing across two olympiads with different papers. The page ends on the results; the only honest button would be one that does nothing, and a disabled *Goşul* reads as a broken contest rather than a finished olympiad. The header's bal pill is hidden on this tab: nothing here pays points.
    - **A place is a place inside a grade.** Each grade is its own board behind a `ChipRow` (opening on the reader's own grade when they sat it), because an 8th-year and an 11th-year sat different papers — ranking them together invents a competition that never happened. Places are **computed from the percentages** with standard competition ranking (equal scores share a place, the next one skips), never stored: a stored rank is a second copy of the result, free to disagree with it. `ResultRow` is `RankRow`'s shape with the points pill replaced by a percentage and its meter — out of a known 100, coloured by `scoreColor`.
    - **Halkara olimpiadalar sits under the results, not in a section of its own.** It is the one part of the tab that has not happened yet, and its reader is the pupil who just scored 96% in the welaýat tapgyry. Each entry ([`IntlOlympiad`](src/data/guides.tsx) → [`IntlOlympiadScreen`](src/screens/DetailScreens.tsx)) leads with the three facts a family gives up looking for — **which stage qualifies you** (an `OlympiadStage`, so it is worded exactly as the results are), **when applications close**, and **who to ring** — as the stat strip above the prose, with the numbers as real `tel:` links and a sticky *Jaň etmek*. One accent in that strip, and it goes to the deadline: the stage is a fact, the deadline is what you can still miss.
    - **The card carries the level, not the prize.** Every prize card would print a laptop or a tablet and a column of prizes sorts nothing — everyone wants all of them. What decides whether to enter is whether the questions are within reach, so the slot the points pill has in the other tab holds *Aňsat · Orta · Kyn*, and the prizes stay on the page with room to show them.
    - **Three phases from two dates** (`startsAt`, `endsAt`; `prizePhase`), never a stored `state`. Before: countdown to the start, packs locked. Open: countdown to the end, packs playable, the sticky button is *the next unsolved pack* — entering a running contest is answering, not filling in a form. After: no countdown, no pack list, and the stat strip reports the standing it finished on rather than the reader's live zero.
    - **The points are earned in the open, not awarded offstage.** *Bal gazan* lists ten packs, each three theme banks of one subject (~12 questions) drawn round-robin from the catalogue by [`contestPacks`](src/data/library.ts) — striding a subject-ordered list instead handed out eight Informatika packs in ten. Each pack runs the app's one quiz component with a **stopwatch**, and banks `{correct, total, seconds}` into [`state/contest.ts`](src/state/contest.ts); re-solving keeps the better run, because a second attempt is practice. Total bal, time spent and packs done are summed from those runs — never stored a second time.
    - **The rating shows what the reader earned.** Their row is their own points sorted in with everyone else's and the whole table re-ranked, so solving a pack visibly moves them; a seeded 1000 would go on claiming a standing for someone who has not answered a question.
    - **Prizes are objects, and objects get a picture and a page.** [`PrizeArt`](src/components/PrizeArt.tsx) draws them in the app's own stroke weight — the app has no image pipeline, and a generic laptop with the model written under it is the honest form for a drawing standing in for a product. Each row opens a prize page (the drawing large, the sponsor who put it up, how it is won); sponsors stay **named, not shown as logos**.
  - **Öwrediji kartlar picks a subject first.** 46 decks listed flat is 46 rows reading "Informatika · 3-nji synp kartlary", with the word *kartlary* on every one of them under a page already titled Öwrediji kartlar, and Informatika on twelve. The page is 20 `SubjectTile`s under the school's own shelves (Takyk · Tebigy · Ynsanperwer · Durmuş we zähmet, grouped by accent family in [`curriculum.tsx`](src/data/curriculum.tsx) so a subject cannot be filed two ways), and the grades sit one level down — the same landing → ders → detail that Testler uses. A tile's second line is the two values the catalogue knows and the reader cannot count (`556 kart · 1–6 synp`), never a phrase restating what the page is. **A locked tile keeps that line**: the grey card and the lock badge already say it is shut, twenty tiles reading "Zehinli bilen açylýar" is one sentence printed twenty times, and the size of the bank is the reason to open it.
- **Three levels, never two** (Gollanmalar): tile grid → section list → **detail page** ([`DetailScreens.tsx`](src/screens/DetailScreens.tsx)). No list row commits you to anything: tapping a contest, game, book, deck or test opens a page that says what it is, how it is scored and how you did last time, and only its sticky footer commits. All five details share one skeleton — `Hero` (tinted panel: icon, state pill, title, meta) → 3-up `StatTile` strip → prose `Card`s (`Steps` for anything ordered) → list(s) → `StickyFooter` CTA — so the sections read as one family. Testler and Öwrediji kartlar add a middle level: landing → ders → test, and landing → ders → toplum.
- **List and detail read one record** ([`src/data/guides.tsx`](src/data/guides.tsx)): every section's rows and its detail pages come from the same array, so a card can never advertise a number its own page contradicts. Derived figures (page number from `read`%, `N/M baplar`, deck percentage, attempt counts) are computed, never restated.
- **Drill-down data**: stat cards expose a full-width contained Button that opens a `SheetDrawer` with detail (subject bars).
- **Roadmap** ([`src/screens/RoadmapScreen.tsx`](src/screens/RoadmapScreen.tsx)): winding node path (SVG connector centred at 50%, nodes swing ±54px), grade bands 1–12, level card, search + activity-type filter chips. Floating jump button shows **one direction at a time**: below own grade → `↓ next`, past it (or at the end) → `↑ previous`; targets recompute from the grade currently in view (scroll-spy). Structure rules: **land on the current lesson** (auto-centred on mount); **fully-completed grades collapse** behind the same band (one band style for all grades and both states — green check + "N sapak tamamlandy" caption when complete, chevron flip is the only open/closed difference; collapsing scrolls the band back into view; search/filter auto-expands); grade bands are **sticky** while their section scrolls; no-match search/filter shows an empty state with an "Arassala" reset.
- **Activity kinds** (roadmap): every lesson is `text | video | interactive | test`, mapped to icon + accent (`KIND_META`): Tekst→blue books, Wideo→purple player, Interaktiw→teal gamepad, Test→orange check. Node states: **done** = accent fill, white icon, green corner check; **current** = white fill, accent border, pulse + bobbing "BAŞLA" pill; **locked** = gray fill, lock corner badge, dotted connector. Completed connectors draw in blue. Tapping any node opens a lesson `SheetDrawer` (type badge, extent, status, `Başla`/`Gaýtadan gör`/disabled `Gulply` CTA).

- **Continue card** (roadmap): the primary CTA — next lesson (kind icon, title, extent) + "Başla" pill, with the grade progress bar folded in; taps open the lesson sheet.
- **Lesson pages** ([`src/screens/LessonScreen.tsx`](src/screens/LessonScreen.tsx)): full-screen per-kind content — video (player mock + progress), text (structured reading: paragraphs, formula callouts, worked example, "Ýatda saklaň" note), interactive (**ParabolaLab**: a/b/c sliders → live SVG parabola, D readout, root markers, three gamified missions gate completion), test (starter page → one question per step with Yza/Indiki → score ring + per-question review). Footer CTA gates on completion rules; completing feeds `doneIds` in the roadmap so the path, badges, captions, and continue card advance live.
- **AI helper** ([`src/components/AiHelper.tsx`](src/components/AiHelper.tsx)): `AiFab` (floating sparkle button on every lesson page, lifts above the footer) opens `AiChatSheet` — a bottom-sheet chat with greeting, per-kind ready prompts (analyze result / quiz me / explain simpler / why needed / give assignment), scrollable thread, pinned input. Replies are mocked; swap `ask` for a model call.
- **One student everywhere**: `STUDENT` in [`src/screens/OtherScreens.tsx`](src/screens/OtherScreens.tsx) (Muhammedow Muhammet, 8-nji «B», 16-njy mekdep) feeds Çagam, Testler, the leaderboard, and Profil.
- **Profil** (4th tab): identity (avatar + edit affordance, name, school·class, "Maglumatlary üýtget" — all three lead to the same edit page) → a 3-up `StatTile` strip answering *where do I stand* (ortaça baha · bal · synpda orun) → payment card (plan + status pill, then Taryh / Kartlar / Tölemek, each a real destination) → the Ýetişik heatmap → goals (favourite subject, dream speciality — both open the same `ChoiceSheet`) → one "Sazlamalar" row into the account tree.
  - **A goal that asks the question it was supposed to answer needs a second door.** "Arzuwymdaky hünär" was a picker of jobs — right for a student who already knows, useless for one who does not. *Hünär synagy* ([`CareerTestScreen.tsx`](src/screens/CareerTestScreen.tsx), data in [`src/data/career.tsx`](src/data/career.tsx)) is the other door and it ends by writing into that same goal, so the two are one decision with two routes rather than two features. The picker carries the escape hatch — "Bilemok — synagdan geçeýin" — because the moment a reader opens a list they cannot answer is the moment to offer it.
  - **The store keeps the answers; the ranking is computed** ([`src/state/career.ts`](src/state/career.ts)). A stored result is free to disagree with the answers that produced it, and it could not re-rank when the speciality list grows. Same rule as marks, attendance and the badge feed.
  - **A result has to explain itself.** One statement per screen (a grid of twelve rows with three radios each is a form, not a question being asked); Yza goes back a *question*; and the result shows the match percentage, the two traits that earned it with their own scores, the school subjects it rests on, and the full six-trait profile it was computed from. The caveat that it is advice and not a verdict is stated **before** the test, not after — a result nobody was warned about reads as a judgement.
  - **Free for every tier.** It is the student's own data about themselves, and gating the one feature that answers "what should I become?" would be the app at its worst.
  - The tab shows **standing, not a grade log** — per-lesson grades live in Gündelik and Analitika, so Profil carries no "recent grades" list to go stale beside them.
  - **Heatmap rules**: the term switch is a `Segmented` control that actually swaps the data (I–II çärýek is a finished term, filled edge to edge; III–IV stops at today's cell), the caption above it (`N baha · N işjeň gün`) is summed from the very cells being drawn, and the filter is a labelled chip ("Diňe köp bahaly") rather than an unexplained two-letter switch. The swatch legend stays passive — never styled like a control.
- **Sazlamalar** ([`SettingsScreens.tsx`](src/screens/SettingsScreens.tsx)): the header gear is the single entry; the tab itself never repeats settings rows. Three groups — Hasap (profil maglumatlary · abuna we töleg · gizlinlik we howpsuzlyk), Programma (bildirişler · dil · ýady arassala), Kömek (one row: **Kömek merkezi**) — then "Hasapdan çyk" alone, in `redText`, behind a confirm sheet. Rules that hold across the tree:
  - **Preferences outlive navigation**: switches read from a module-level store (`usePrefs`), so a toggle set here is still set after backing out and returning. A setting that forgets itself reads as a bug.
  - **A master switch dims what it governs** instead of silently ignoring it — the child rows go `disabled` + 50% and show off (Bildirişler).
  - **Read-only fields say who owns them** ("Synpy we mekdebi mekdep dolandyrýar") rather than being mysteriously inert.
  - **Destructive and money actions confirm in a sheet** (log out, cancel subscription, pay), and the leaves that genuinely need a backend end in one toast instead of a dead row.
  - **A saved payment method is a card; the other two ways to pay are not saved** ([`src/state/payMethods.ts`](src/state/payMethods.ts)). A gift card is a code spent once and a phone transfer is something the reader does from their own phone — neither has anything worth keeping between payments, so both are offered in the pay sheet at the moment they are useful rather than being added to a list of "methods". Töleg usullary holds cards, and says in one line where the other two live so nobody hunts for them there.
  - **Only the bank is named** (Halk bank · Senagatbank · Rysgalbank). Each issues several different cards, so printing one product name beside the bank would be wrong for most of the plastic people hold; a saved card identifies itself by its last four digits, and the entry form's live card preview wears the chosen bank's colour.
  - **For a phone transfer the app is the payee, so it shows *our* numbers** — one per operator, each with its own "Töle" beside it — instead of asking for the reader's number. The pay sheet and the payments page read the one store, so the method about to be charged can never be a stale string printed in two places.
  - **Help builds from the row set, not from new shapes.** The support entry is a `SurfaceRow` like every other destination, the guides are `SurfaceRow`s, the "?" is the `HelpButton`. A bespoke card for the support entry made the one row that is not new look like the one row that matters, and a second "?" shape taught the reader that the affordance they had already learnt was something else here.
  - **"I am stuck" is one intention, so help is one row and one page** ([`HelpScreen`](src/screens/SettingsScreens.tsx)). It was four — FAQ, materials, goldaw, and a version number hiding two rows below them — which made the reader choose which of the four holds their answer before they know what kind of answer it is. The hub runs in the order a person actually tries things: talk to a person (a card that says whether anyone is there *right now*), read a guide, look up a question, then what the app itself is.
  - **The guides are split by who is asking, not by format.** This is four different apps depending on who opens it: a parent signs and reads, a student does homework, a teacher records marks, a principal publishes and reports. One "how to use Gündelik" list is wrong for all four — the parent scrolls past mark-entry, the teacher past signatures. The role `Segmented` is the first control on the page, and it moves the **FAQ with it**: "baha nädip üýtgedilýär?" has a different answer depending on which side of it you sit. The full question list stays one tap away for anything the role split misses.
  - **Goldaw answers three things before anything else**: is anyone there *now* (a live dot from the clock against opening hours), how long a reply takes, and which channel to use — in that order, above the channel list. It replaced a row at the foot of the FAQ that raised a toast: the app's one honest dead end, on the screen people reach *after* the answers failed them. The account block at the foot (version · plan · account id) exists because that is the first thing an operator asks for and the last thing a parent can find; one button copies the lot.
  - **A guide is a short article that shows the control it is explaining** ([`GuideScreen`](src/screens/HelpScreens.tsx)). Read time, a sentence of what the thing is, the thing itself, then what to do with it — no hero, no cover art, no author. Every demo is built from the *same* components the app draws with (`GradeBadge`, `BadgeScore`, `ToggleSwitch`, the homework chip's own shape), so a guide cannot teach a button that no longer looks like that; a screenshot would go stale the first time a radius changed. The demos are inert on purpose: an article is a place to recognise a control, not to operate one.
  - **Guide rows carry no second line.** The title is the subject and the read time is the value — a sub-line paraphrasing the title is the row-subtitle rule broken by the very pages that document the rules.
  - **"Programma barada" is its own menu, not the foot of the help page.** What the app *is* and what changed in it are read for a different reason than "how do I sign a day?" — usually before installing, or when something looks unfamiliar after an update. Bolting them under the guides made the help page end twice. The card carries what it is and which version, and then **names the four users separately** — ene-ata, okuwçy, mugallym, mekdep müdiri, each with the one line that says what they do here. "4 ulanyjy görnüşi" was a count that hid the thing it counted, and every one of the four opens this page wanting to know whether *they* are in it. There is no language row: the app is Turkmen, and a fact that never varies is decoration.
  - **Täzelikler is a changelog, not a splash** ([`UpdatesScreen`](src/screens/HelpScreens.tsx)). The current version is open and the history is collapsed behind its own headline and a change count — a changelog is read for "what changed for me *this* time", and three versions unfolded at once is a wall nobody reaches the end of. Every release carries a one-line headline, because "is this one worth reading?" has to be answerable before the list is.
  - **Inside a release: new things, then improvements, then fixes** — grouped, with the tag as a *heading over its group* and the changes as a tone-dotted list beneath it. The tag was tried twice before that: a tinted pill on every line (a second grid of coloured boxes competing with the sentences it labels), then the bare word in a fixed left column, which costs the same ~76px on every line of a 375px screen and wrung every sentence into a narrow ribbon down the right-hand side. Written once as a heading, the changes get the full width they are read at. The tag matters at all because "improvements and bug fixes" is the sentence that taught everyone to stop reading release notes. One version string (`APP.version`, derived from the newest release) feeds the settings row, the about card and the support block — three places that could otherwise print three different versions.
  - **Synag** is the last group and holds only the two switches that change what the whole app may show: *Mugt hasap rejimi* (drops the entitlement, and resets the weekly meters so free mode starts on a fresh week) and *Beta aýratynlyklar*. One caption under them says what each one turns on.

- **Entitlement is a tier, not a boolean** ([`src/state/prefs.ts`](src/state/prefs.ts)): `usePrefs()` owns `tier`, `beta`, every notification/security switch, the language and the weekly free meters (`usedTest`, `usedCards`). With one paid plan a boolean was the whole model; with two it cannot answer "may this account see Analitika?", because one paying account may and the other may not.
  - **`FEATURES` is the single matrix**: each feature names the lowest tier that unlocks it, and the comparison table on the tariff page *and* the screens that gate themselves both read it. A row that promises a feature and the screen that locks it cannot drift apart.
  - Screens ask **`useCan('analytics')`**, never `premium &&`. `premium` still exists but is *derived* (`tier !== 'free'`) — a second stored copy would be free to disagree.
  - **Two plans, named after the student, not the price**: `Göreldeli` (5 TMT/mo — bildirişler, nyşanlar, testler, bäsleşikler) and `Zehinli` (50 TMT/mo — everything). A tier a family can say out loud is one they can choose between. `ENTRY` is the cheapest way in and is what ads and teasers quote.
  - **A year's list price is derived** (`monthly × 12`), so the discount printed beside it can never contradict the monthly figure above it.
- **Free tier: state the limit where the choice is made, and always name what remains.** The meter appears on the section page (Testler, Öwrediji kartlar) *and* the detail page that spends it — not only after it is gone. Locked grades say so in the roadmap band ("Premium · 20 sapak") rather than waiting for the lesson sheet. What the free tier keeps: the diary and grades in full, the first teacher note, badge totals, 1-nji synp of the roadmap, one test and one card session a week.
- **A teaser shows the real thing, blurred.** `LockedPreview` blurs actual content (the next note, the real per-badge bars) so the offer is legible; a placeholder box would sell nothing.
- **Lock on value, not on volume.** The strictest wall in the app is the badge history for free accounts (`LockedFeed`): the *count* and the *date* stay free, and the type, subject, teacher and comment are what Premium buys. A parent can see that four things happened today and not one of them says what — a specific gap, which is far stronger than a wall, and everything on screen is still true. No invented scarcity, no countdown, no fake urgency: the number of hidden rows is the real number.
- **Analitika is gated, and the gate shows the shape of what is behind it** ([`AnalitikaScreen`](src/screens/OtherScreens.tsx)): four real cards, blurred, each captioned with a true count — "3 hepdelik taryh", "7 ders". The count is free, the reading is paid, same line the badge history draws. The streak stays fully visible because it is counted from grades the diary already shows in full, and hiding a reader's own data back from them buys nothing. No countdown, no invented scarcity.
- **A tab full of derived numbers earns a "?"**: Analitika's header carries the same on-demand help as `SubPage`, because "where does this come from?" is the first question a computed figure provokes.
- **The tariff page carries one decision.** Price choice first, then the comparison table, then one action ([`UpgradeScreen.tsx`](src/screens/UpgradeScreen.tsx)). It used to also carry a stat strip, four highlight tiles and two reviews — three more blocks all saying "Premium is good", two of them restating rows of the table below, which buried the price under three screens of scrolling. Selling the same point four times reads as pressure.
  - Term is settled first and is deliberately the *small* control — it is the cheap decision, and settling it lets each plan card show one price instead of four numbers competing.
  - **In the table a row is a whole feature, so a lock is literally accurate** — `✓` or a lock, never a text value. With two paid plans the table has three mark columns, and the one you have selected is the one highlighted. Hedged cells ("hepdede 1") are unreadable at a glance and forced every feature to be phrased as a quantity; "Çäksiz testler" locked is the same fact, legible.
  - The yearly saving is **derived from the two prices**, so a discount claim can never contradict the numbers printed beside it.
- **Homework is a to-do list, in both places it is read** ([`TodoRow`/`TodoList`](src/components/Ui.tsx)). It used to be prose in two shapes — a paragraph in the day sheet, another in the lesson sheet — with the ticking done by a button at the bottom of the sheet. One row now: a box, the task, and what it belongs to, with the day's progress as a bar above the list. It **unticks** (the mock API takes the state, not just "done"), because a checkbox that only goes one way is a trap people learn not to touch, and the lesson sheet's duplicate "Ýerine ýetirildi diý" button is gone — one state, one control, and the control is the task itself.
- **The account has a balance, and the balance is the point of sale** ([`state/wallet.ts`](src/state/wallet.ts)). Money here arrives in lumps — a phone transfer, a kiosk gift card — so the app holds a balance and the subscription is spent *out* of it (`BalanceNote` says so above every buy button, including how much is missing). The balance is the **sum of its entries**, never a stored total beside a stored history.
  - **Points convert to money**: 100 bal = 1 TMT, whole TMT only, the remainder stays on the account. Points that only ever bought a place in a rating are a scoreboard; points that become a fortnight of Zehinli are a reason to open the app on a Tuesday.
  - **Schoolwork pays** ([`state/earn.ts`](src/state/earn.ts)): +5 bal a homework, +20 a test, idempotent by id so re-ticking and re-sitting cannot be farmed. It is a **paid** feature, and the free tier is shown the figure greyed with a lock rather than nothing at all — a reward you can see is the paywall's clearest argument.
  - **The shop** ([`data/shop.tsx`](src/data/shop.tsx), [`WalletScreens.tsx`](src/screens/WalletScreens.tsx)) spends the balance on real objects from the contests' own named partners, grouped by shop rather than by category, with the balance pinned above the prices. Every item names whose counter it comes from; an order keeps a six-digit code, and no order exists without a payment.
- **The class answers the evening's other question — and only half of it** ([`ClassScreens.tsx`](src/screens/ClassScreens.tsx), roster in [`data/classmates.ts`](src/data/classmates.ts)): "has anyone else done it" is what a pupil asks straight after their own list. The page shows **only the pupils who have**. A list of names under "has not done their homework" is a wall of shame, it is the one screen a parent would show a child as a threat, and it tells the reader nothing they can act on.
  - **It is an order, not a roster**: three names with the time each finished, then a number ("we ýene 9 okuwçy tabşyrdy"). Naming the fourteenth pupil to hand in is data, not information — a class list is not a leaderboard past the podium.
  - **The reader's own place is the top line, and it is free**: "Siz 4-nji bolup tabşyrdyňyz". It is a fact about them, ticking the task is what fills it, and it means the reward for doing the work is the first thing the screen says. The three names are what the subscription buys.
  - Who did what, and when, is derived from one honest number per pupil (`rate`) plus a hash of (pupil, task) spread across 15:00–22:00 — stable for the life of the app, so the same class never reshuffles between openings. The reader's own row is their real tick, never a guess.
  - `RankRow` grew an optional `points` and an `end` for this: a placing is not always a score. Contests rank by points and say so; homework ranks by who finished first and shows the time.

- **One account, several children** ([`state/children.ts`](src/state/children.ts)): the selected child is one value the whole app subscribes to, not a prop drilled through the shell, so the diary, the profile, the analytics header, the class list and the share card cannot end up describing two different children. There is no "the student" any more — `useStudent()` and `useGrade()` replaced the `STUDENT` constant and `USER_GRADE`, because a name, a class, an average and a programme year are facts about a child, not about an account. Switching child re-opens Sapaklar on *their* year and re-reads the day from *their* timetable (`fetchDay(key, child)`, one cloned book per child so ticking one child's homework cannot tick another's).
  - **The control is a pill and a sheet, and it took three tries.** A `Segmented` is right for two children and wrong for five — the track divides the width by the number of options, so the fifth child arrives as three clipped letters. A full-width card was readable and read as *content*: a row about the child rather than a way to change them. What works is a small pill that says one thing (the name on screen) opening a list where a name has a whole row and a sixth child costs nothing.
  - **The sheet carries the fact that makes the choice**: each child's class and how much of tonight's homework is in (`hwGlance`). A parent of five opens it to find who needs them this evening, not to read five names they already know.
  - **The pill is the diary's header control, in the bell's place.** Which child you are reading is asked far more often than "what is new", and a root screen holds one control — so Habarlar moved *inside* the sheet, as its first row with the unread count it had on the bell, and the pill carries a red dot while anything is unread. The bottom of a five-row list is a place people scroll past; an unread count nobody meets is a notification that did not happen. Analitika has no switcher at all: it is one tab from the diary, and a control repeated on every screen is one the eye stops reading. Profil keeps `ChildPickerRow` in its account list, where the identity card already shows who this is.
  - **The selected child does not wear the premium ring.** The ring is the subscription's mark; the check mark is the selection's. One mark cannot mean two things.
  - **The subscription is bought per child**, the way a school sells it: the tier lives on the child (`Child.tier`, `Child.untilIso`), not in `prefs`, because an account-wide tier could only answer for one of five. `usePrefs().tier` and `useCan()` read whoever is selected, so no screen below them had to learn that a family can hold three plans at once — a free child sees banners and teasers on the same screens a paying sibling sees the content. The free plan is called **Adaty**: it is a plan like the other two, and `tierName('free')` is the only place its name is written, after the tariff table and the help chip spent a while calling it "Mugt" while everything else said Adaty.
  - **A list badge drops the verb.** `109 gün` in the child list, `109 gün galdy` on a page. Five sentence-form countdowns in a column push each child's own name into an ellipsis.

- **Banners are the other half of the paywall** ([`BannerScreens.tsx`](src/screens/BannerScreens.tsx), tables in [`data/banners.tsx`](src/data/banners.tsx)): a free account sees one paid 2:1 card per page, always under the content rather than over it, always labelled `mahabat`, and always carrying an ✕ that opens the only thing that removes it — `PaidFeatureSheet` for the `noads` feature. An ✕ that hid the banner for a day would be a lie told by a control.
  - **Tapping a banner opens a sheet, never a page.** An ad that navigates away from the diary has taken something the reader did not offer. The sheet is the card, then the offer on a `SnapSlides` track, then the phone number and the address as ordinary rows.
  - **The slot is stable.** `bannerFor(placement)` is a hash of the placement, so a page shows the same campaign every time it is opened; an ad that reshuffles on re-render reads as a broken screen.
  - **The seller sees the same three tables the reader does** (`PLACEMENTS`, `AD_SCHOOLS`, `BOOKED`): what a day costs, how many schools it reaches, and which windows are sold. Availability is a **lookup, never a guess** — booking a single school collides with a booking of "every school", because one slot cannot show two banners at once — and an unavailable window is only half an answer, so the form also offers the first date that would work, as a button. A booking spends the same balance the shop spends and joins `BOOKED` immediately, so the day you buy stops being offered a minute later on the same form.
- **The free tier gets a go a day, not a wall** ([`state/allowance.ts`](src/state/allowance.ts)): one deck and one test, metered on *opening* rather than on browsing, and spent on that item so returning to it is free. The catalogue stays fully visible — a grid of grey tiles hides the very thing that argues for paying — and the teaser says "today's is used, tomorrow there is another" instead of "locked".
- **A paywall over someone else's writing shows the register, not a sample.** Teacher notes on the free tier list every author, subject and time and hide only the words. The old behaviour — first note in full, rest locked — gave away the thing being sold and still left the reader guessing how much they were missing.
- **The subscription is sold four ways, on purpose.** One offer — same `TIERS`, same `FEATURES`, same `setTier`, all of it in [`payBits.tsx`](src/screens/payBits.tsx) — presented four ways, reachable from the ✦ in the tariff page's header. A variant may differ in what it shows and in what order; never in what it sells or what it costs.
  - **Reference** ([`UpgradeScreen`](src/screens/UpgradeScreen.tsx)) — term switch, two plan cards, the full comparison table. Everything visible at once, for a reader who has already decided to pay and wants to know exactly what the plans differ by. A poor first pitch: the table is a document.
  - **Bir teklip** ([`PayOfferScreen`](src/screens/PayOfferScreen.tsx)) — one plan, one price, one button. Bets that most people do not want to choose a plan, they want to be told what it costs and whether it is worth it. The other options stay as two quiet lines under the button, in the voice of an aside: a page that *hides* them is not simple, it is evasive.
  - **Deňeşdirme** ([`PayCompareScreen`](src/screens/PayCompareScreen.tsx)) — the two plans side by side, each with its own price and its own button, and under each **only what that plan adds**: the dearer card says "everything in Göreldeli, plus…", which is one line where the table needed seven repeated ticks. Both cards buy on the spot — a comparison that ends in one button below it makes the reader carry their choice down the page and hope the button remembered.
  - **Näme açylýar** ([`PremiumScreen`](src/screens/PremiumScreen.tsx)) — value first, price second: emblem, one plan and one term, then ten promises you can open. Opening one mounts all ten on a horizontal scroll-snap track (the shell's own mechanism), so a reader who opened one out of curiosity can swipe the rest; the dots are that position and they are tappable, because a dot you can see and cannot press is a decoration pretending to be a control.
  - **The pitch is not a different app.** An earlier draft of the value page was dark, on the theory that an ad should not look like the product it sells — one screen in its own mode is a different app wearing the product's name, which is a strange thing to hand someone at the moment you ask them for money. All four are the same daylight, the same `IconBadge` pairs, the same green saving badge and the same primary button. The only thing that is special is `PREMIUM_GRADIENT` (blue → purple, both tokens), the mark of the thing being sold: the emblem, and the ring on a subscriber's avatar.
  - **Constraints every variant inherits**: prices and the saving are derived from `TIERS` (a badge can never contradict the figure beside it); every bullet is a `FEATURES` label, so a promise cannot outrun what the app unlocks; the free baseline is stated rather than implied; the trust line under the button is identical everywhere, because what happens next and how to get out of it is not a thing to A/B.
  - They live behind one pill rather than a split test, because nobody has measured anything yet and a split nobody reads the numbers of is a coin toss with extra code. **When there are numbers, the winner replaces the reference page and the rest are deleted.**
- **Habarlar** ([`InboxScreens.tsx`](src/screens/InboxScreens.tsx), data in [`src/data/inbox.tsx`](src/data/inbox.tsx)): one hub for everything arriving from outside the student, switched by one `Segmented` with live counts, reached from the bell in the Gündelik header. They share a page because they answer one question — "what is new for me?"
- **Akylly mugallym is a chat** ([`AiChatScreen.tsx`](src/screens/AiChatScreen.tsx), store in [`src/state/aiChats.ts`](src/state/aiChats.ts)), so it lives in the Söhbet list — pinned above the teachers, tinted, never carrying an unread. A student looking for "what did I ask about diskriminant" looks where their other conversations are.
  - **Conversations are records, not sessions.** The AI used to live only in a lesson sheet: you asked, you closed it, the answer was gone. The store is a module `useSyncExternalStore` like the rest, because the page unmounts every time it is closed.
  - **History is one header icon, not a list above the thread.** You are almost always continuing the conversation you are in. Opening history is also the only place a new conversation starts — "back to an old one" and "start a new one" are the same decision.
  - **A conversation is titled by its first question.** Nobody names a chat, and "Täze söhbet · 3" is not a history.
  - **Two surfaces, one memory.** The lesson-page sheet stays, because a question asked while reading a lesson has context the standalone page lacks — but every message it exchanges is mirrored into the same store. Two histories would mean the answer you remember is in neither.
  - **Bildiriş is announcements, never activity.** A notification stream that echoed grades, badges and homework was a second, worse copy of the diary. It now carries only what a school office publishes (`mekdep · okuw · çäre · duýduryş`), grouped by day and always naming the office that published it. Nothing in it is an action.
  - **An announcement row leads with the sender.** Order is *kind pill · who sent it · time*, then the headline, then the body. The rows used to open with a 44px coloured icon per category — four glyphs that all meant "an announcement", carrying less than the category's own name does in a 20px pill. Who published it is what changes how urgently the rest is read: "Okuw bölümi" and "Mekdep müdirligi" are different authorities.
  - **The list is a list; the announcement is a page.** Rows carry sender, time, headline, two clipped lines of body and the file count. An announcement can run to three paragraphs plus a timetable, and a list that shows all of it is not a list — it is the page. Opening a row marks it read and gives the full text (`white-space: pre-line`, paragraphs intact) with the files named as rows, same `FILE_KIND` map as chat so one file never gets two descriptions.
  - **No category badge on the row.** It was an icon first, then a tinted pill, and both were a label on a list where every item is the same kind of thing. The category is on the detail page, in the line that already carries the date.
  - **Söhbet is real conversation**: teacher threads, the class thread, and the parents' group. Group threads print the author's name **only when the speaker changes**, so a run of messages from one parent doesn't repeat a name four times. "Mugallyma ýaz" opens the class's teacher list and either resumes an existing thread or starts an empty one — a new conversation is never a dead end.
  - **A file is a card, not a bubble**: name, kind and size, so the reader decides before downloading. The kind's colour *and* glyph come from one `FILE_KIND` map, so a bubble and the attach sheet can never describe the same file two ways.
  - **The composer scrolls the page to its true bottom, not the last bubble into view.** `scrollIntoView({block:'end'})` lines the anchor up with the bottom of the scroll container, which is exactly where the sticky composer sits — the newest message ends up hidden behind the bar it was just typed into. The thread also claims `calc(100dvh - 340px)` so a three-message conversation still puts the composer at the bottom of the screen instead of floating it mid-page.
  - **Makala is CMS content.** Articles are written and published by admins, not by the app — so there is no featured slot and no cover art to invent, just title, byline, date and read time. Categories still get their own mark rather than only their own tint.
- **Mugallymyň nyşanlary** ([`BadgeScreens.tsx`](src/screens/BadgeScreens.tsx), data in [`src/data/badges.tsx`](src/data/badges.tsx)): teacher badges are the qualitative counterpart to grades — *what was done* vs *how it was worked*. Positive and negative are one scale, never two lists: `BadgeScore` (`+3 / −1` in tone-tinted numerals) is the compact form everywhere, and the stats page opens on totals + a single green/red balance bar before breaking down by type and subject. A subject with no negatives shows `+2` — never `+2 · −0`.
  - The two tones are **Ýagşy** and **Üns bermeli**. Not "Bellik" — that word already means a teacher's *note* in the diary, and one word cannot carry two meanings one screen apart. "Üns bermeli" over "Käýinç" because the negative tone describes something to work on, not a reprimand handed down.
  - **An emoji needs a label.** The badge emoji are the badge's identity in a labelled list (the history feed, the type bars). On a lesson card, unlabelled, an emoji disc next to the geometric `GradeBadge` reads as decoration in a different visual language — so the card uses `BadgeScore` and the sheet carries the words.
- **A referral code is read aloud, texted, and typed by someone else** — so it is a word plus three digits (`ALTYN-472`), not a hash. `a7Kq2xB` does not survive a phone call.
- **The referral bonus is credit, not cash**: it pays the subscription or buys school supplies at named partner shops. That changes the page — no "withdraw" button and no payout minimum, because a balance that can only be spent has no threshold to clear. The card carries the two real destinations instead of one blocked one, and says plainly that it is not withdrawable rather than letting a reader assume a bank transfer is coming. Partner shops are **listed by name**, because "partner stores" is a promise and a list is a fact.
- **Dostuňy çagyr** ([`ReferralScreen.tsx`](src/screens/ReferralScreen.tsx)): the one illustrated page in the app, and the page order is the order the questions come in — what do I get (the illustration and the headline), what do I have (the balance, directly under it), what do I do (the code), how does it work (three steps), who came (the friend list, with its two counts sitting *with* the list rather than in a strip at the top of the page).
  - **The illustration has one subject and it fills the frame.** It went through a near-facsimile five-manat note (serials, blind marks, microtext, a cameo silhouette — all rendered ~110px wide, where every detail collapses into a khaki smudge), then a simplified note sharing the frame with two avatar circles and a dashed arc, which left the scene busy and the subject unclear. What works is the note drawn *structurally* — engraved inner frame, one big numeral with the word beneath, portrait vignette, emblem, göl band, the two colour strips down the right edge — at a size where each of those reads, with a second note behind it for depth and two coins. Nothing smaller than the word MANAT.
  - **The note's palette is the only thing in the app that is not from `tokens`**, deliberately: colour is most of what identifies a denomination to someone who handles it daily, and a token green would read as generic play money. The numeral is `PLAN.referralReward`, so the drawing cannot contradict the offer written under it.
  - The code box is dashed and tappable, the three steps say when money actually lands, and there is no payout button — see the credit-not-cash rule above.

## Conventions

- No hardcoded hex in screens — colors come from `tokens`.
- **A row's second line is one line.** `SurfaceRow`'s sub is `noWrap`: it wrapped to two and three lines beside a trailing figure and read as a collision (`Balans` under `110 TMT`). A setting's consequence still wraps — that is `SwitchRow`, and explaining what a switch will do is not the same job as stating a value.
- **A single-column stack is `flex` or an explicit `minmax(0, 1fr)` grid, never a bare `display: grid`.** A bare grid's implicit column is sized to max-content, so one `noWrap` line inside it silently widened every row in the stack ~90px past the phone frame. The overflow only appeared when the text stopped wrapping, which is exactly the kind of latent break that ships.
- **A row's second line is for a value it cannot show any other way — never to explain what the row does.** The label already says that; a subtitle repeating it in other words is noise that doubles the height of every row. Put current state in `RowEnd` on the right (`Dil ⟶ Türkmen dili ›`) and drop the rest, so a menu reads as a list of destinations. When every row in a list shares the same title ("Premium · aýlyk" six times), the varying field is the row's identity — promote it to the label and let the amount be the value.
- **Red means unread, and nothing else.** A count that is only a quantity (12 teacher notes) takes `CountPill tone="quiet"`; two stacked rows must never both shout. Every unread count is the *same* `CountPill` — a chat row that rolled its own blue pill made unread mean two colours on one screen.
- **Data stores an ISO date; only [`lib/date`](src/lib/date.ts) formats one.** The mock used to carry pre-formatted Turkmen strings — "4-nji fewral", "8-nji few.", "30-njy ýan." — three spellings of one idea, none sortable, all long enough to truncate in a row. The rules now live in one place:
  - **`DD.MM.YYYY`, never an ordinal month name.** `04.02.2026` is scannable, fixed-width and sorts by eye.
  - **Relative wins at ±1 day and loses past it.** `Şu gün` / `Düýn` / `Ertir` are read faster than a date the reader has to subtract; "3 gün öň" makes you do arithmetic to place it in a week, so day 2 onwards is numeric. `fmtDate` picks; `absDate` forces numeric for tables; `relDate` returns *only* the word (or `null`) for places already showing a date.
  - **Inside today, a message shows the clock**; yesterday shows `Düýn`; older shows the date (`fmtWhenShort` / `fmtWhen`).
  - **One anchor**: `TODAY`. Weekday names, day numbers, "is this today", the week strip, the last-sync stamp and the timestamp on a message the user just sent are all derived from it — including in mock data, so a real `new Date()` can never drop an August row into a February screen.
- **Bookmarks are one store, not one per section** ([`src/state/bookmarks.ts`](src/state/bookmarks.ts)): six sections hold six kinds of resource, but a reader's own shortlist crosses them — the half-read book and the contest they meant to enter belong on one page. The key is `kind:id` because ids are only unique within a section. `BookmarkButton` lives in the **detail page header**, never on list rows: saving is a decision, and the detail page is where there is enough to make it. Filled means saved — a stateful control has to look different in its two states.
- **The nav hides on inner pages.** A page reached by a back button owns the screen; a bottom bar under it offers a second, competing way out of somewhere the user got to by drilling in, and the back button is the one that preserves where they were. `PillHeader` calls `useSwipeLock()`, so "has a back button", "can't be swiped away from" and "hides the nav" are one fact rather than three that can drift. The panel's bottom padding drops with the bar — leaving 104px reserved would strand every sticky footer above a band of nothing.
- **One icon treatment per row group.** In a list of sibling rows, either every icon sits in an `IconBadge` or none does — a mix reads as two different components.
- **Different things get different glyphs.** The attach sheet offered three choices behind three identical paperclips, which told the reader nothing about which row did what; it now uses `ImageIcon`, `DocIcon` and `CameraIcon`, and the paperclip stays the *entry point* to attaching. The same rule caught a map `PinIcon` standing in for an attachment — a pin means *place*.
- **A sub-page opens at its top.** Sub-pages swap into the tab's own scroller, so `SubPage` resets it on mount; otherwise a page opens at whatever offset the previous one was left at.
- **`IconBadge` must stay visible against what it sits on.** A neutral badge on a `SurfaceRow` uses `surfacePress`, never `surface` — that's the row's own fill, and the squircle vanishes, leaving a bare icon floating beside neighbours that have a container.
- **One icon weight per badge.** Badge glyphs are stroked at 1.8; the *filled* `QuestionIcon` reads as a solid disc next to them, so badges use `QuestionOutlineIcon` and the filled one is reserved for standalone hint buttons on a plain surface.
- **`size` means optical size, not box size — and the factors are measured, never guessed.** These glyphs come from the source design with inconsistently cropped viewBoxes: some are trimmed tight to the artwork, others carry generous padding, so a `size` prop sets a *box* that says nothing about how big the icon looks. Measured at `size={22}`, ink ran from **11.7px** (Flame) to **26.2px** (Chevron — taller than its own box): a **2.25× spread inside one nominal size**. So every icon carries an `optical` factor derived by rendering it, reading `getBBox()`, and scaling so the geometric mean of the ink lands on 15.8px at `size={22}` (~72% of nominal, the usual 24-grid convention). That brings the set to a **1.003× spread**. Re-derive the same way if a glyph is replaced; hand-tuning a few icons by 5–10% is noise against this and will not fix it.
- **An icon's ink is `currentColor`.** A baked-in hex makes a glyph single-use: the same calendar was blue in a tinted pill and wrong in a white header button, and three icons carried hexes (`#3F7CF2`, `#898D95`, `#5A5E6F`) that were not tokens at all. The exceptions are deliberate and small: the date-strip event marks (`HouseEventIcon`, `PenEventIcon`) are coloured marks rather than icons, and `#fff` is a knockout — a hole in a coloured shape, not ink.
- Interactive elements are `ButtonBase` with an `aria-label` when the visible content isn't text.
- Decorative icons `aria-hidden`; stats/charts get `role="img"` + Turkmen `aria-label`.
- Toasts (`toast(msg)`) confirm every mutating action, and are the **last** resort for a dead end — a row that could plausibly open a page gets the page, not a "Tiz wagtda…".
- A number is stated once and derived everywhere else; two components never restate the same figure from separate literals.
- Scroll containers hide native scrollbars (`scrollbarWidth: none` + `::-webkit-scrollbar`) — the app renders as a phone surface.
- Keyboard focus is visible globally: `:focus-visible` gets a 2px blue outline (App-level GlobalStyles).
