# Where the app fetches its content from — the curriculum, the lessons and the
# interactive mini-apps. Leave unset to use the tree served with the app
# (public/data), which `npm run import:curriculum` writes.
#
# VITE_DATA_URL=https://api.example.tm/v1
